name: Patch fNOS Image for UTM (Standard Kernel)
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y fdisk xz-utils grep sed tar kmod zstd qemu-user-static binfmt-support
          echo "✅ 依赖库与 QEMU 模拟器安装完成"

      - name: Get Latest Release Info and Download
        id: get_info
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          REPO="ophub/fnnas"
          TAG="fnnas_base_image"
          LATEST_ASSET=$(gh release view "$TAG" -R "$REPO" --json assets \
            -q '.assets[] | select(.name | endswith(".img.xz")) | .name' | \
            sed -E 's/.*_([0-9]+)\.img\.xz/\1 &/' | \
            sort -n | tail -n 1 | cut -d' ' -f2-)
          
          echo "VERSION=$(echo "$LATEST_ASSET" | grep -oP '(?<=_)\d+(?=\.img\.xz)')" >> $GITHUB_ENV
          echo "SOURCE_FILE=$LATEST_ASSET" >> $GITHUB_ENV
          gh release download "$TAG" -R "$REPO" -p "$LATEST_ASSET" --clobber

      - name: Decompress and Patch Image
        run: |
          # ... (前面解压、losetup、mount 的步骤不变) ...

          # --- 关键修正：跨架构支持 ---
          # 1. 拷贝 QEMU 解析器到镜像内 (这步是解决 Exec format error 的核心)
          sudo cp /usr/bin/qemu-aarch64-static mnt_new/usr/bin/

          # 2. 准备 chroot 环境
          sudo mount --bind /dev mnt_new/dev
          sudo mount --bind /proc mnt_new/proc
          sudo mount --bind /sys mnt_new/sys
          sudo cp /etc/resolv.conf mnt_new/etc/resolv.conf

          # 3. 现在执行 chroot (QEMU 会自动介入)
          sudo chroot mnt_new /bin/bash -c "
            apt-get update
            apt-get install -y linux-image-arm64
            apt-get clean
          "

          # 4. 提取内核文件
          cp mnt_new/boot/vmlinuz-*-arm64 ./vmlinuz-standard
          cp mnt_new/boot/initrd.img-*-arm64 ./initrd-standard

          # 5. 适配 UTM (fstab)
          # ... (保持原来的 sed 逻辑) ...

          # 6. 清理并卸载
          sudo rm mnt_new/usr/bin/qemu-aarch64-static # 记得删掉拷贝进去的工具
          sudo umount mnt_new/dev mnt_new/proc mnt_new/sys
          sudo umount mnt_new
          sudo losetup -d $LOOP_DEV

      - name: Create README
        run: |
          cat << EOF > README.md
          # fNOS UTM ARM64 (Standard Kernel Edition)
          
          此版本已注入 Debian 标准内核，支持 NFS 服务。
          
          ## 🚀 UTM 配置指南
          1. **驱动器**: 导入 \`rootfs.img\` (VirtIO 接口)。
          2. **外部内核**: 使用 \`vmlinuz-standard\`。
          3. **Initrd**: 使用 \`initrd-standard\`。
          4. **启动参数**: "root=/dev/vda rw console=tty0 console=ttyAMA0 rootwait"
          EOF

      - name: Final Packaging
        run: |
          PKG_DIR="fn_utm_${{ env.VERSION }}"
          mkdir -p "$PKG_DIR"
          mv rootfs.img vmlinuz-standard initrd-standard README.md "$PKG_DIR/"
          tar -cvf - "$PKG_DIR" | xz -z -T0 -v > "${PKG_DIR}.tar.xz"
          echo "FINAL_PKG_NAME=${PKG_DIR}.tar.xz" >> $GITHUB_ENV

      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: fnnas_utm
          name: fnnas_utm_${{ env.VERSION }}
          body: |
            ## 飞牛 OS (FnOS) UTM 虚拟机适配版
            
            **🔥 本版本特性：**
            - **标准内核注入**：集成 Debian 官方 `6.1.0-xx-arm64` 内核。
            
            **🛠️ UTM 导入说明：**
            1. **系统类型**: Linux / ARM64
            2. **内核 (Kernel)**: 选择解压后的 `vmlinuz-standard`
            3. **初始化镜像 (Initrd)**: 选择解压后的 `initrd-standard`
            4. **磁盘 (Drive)**: 导入 `rootfs.img` (接口选择 **VirtIO**)
            5. **启动参数 (Arguments)**: 
               "root=/dev/vda rw console=tty0 console=ttyAMA0 rootwait"
            
            > 注意：系统盘空间有限 (3.1G)，请务必挂载额外的虚拟磁盘作为数据存储。
          files: ${{ env.FINAL_PKG_NAME }}
          overwrite_files: true
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
